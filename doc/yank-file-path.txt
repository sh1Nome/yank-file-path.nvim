*yank-file-path*  Copy file paths and code blocks to the clipboard

  Introduction ........................................ |yank-file-path-intro|
  YankFilePathOptions ........................... |yank-file-path-api-options|
  yank_file_path() ....................... |yank-file-path-api-yank-file-path|
  yank_file_path_code_block() ... |yank-file-path-api-yank-file-path-code-block|
------------------------------------------------------------------------------
                                                          *yank-file-path-intro*
This plugin copies a path relative to Neovim's current working directory to
the clipboard. It can add a line range or include the buffer in a fenced
code block.

Both public functions return the copied text.

------------------------------------------------------------------------------
                                                    *yank-file-path-api-options*
Options for selecting an inclusive, one-based line range.

Specify both fields to copy a line range.
Class ~
{YankFilePathOptions}
Fields ~
{start_line} `(optional)` `(integer)` One-based inclusive start line.
{end_line} `(optional)` `(integer)` One-based inclusive end line.

------------------------------------------------------------------------------
                                             *yank-file-path-api-yank-file-path*
                           `M.yank_file_path`({opts})
Copy the current file path, optionally with a line range, to the clipboard.

With no range, return the relative path. With a valid range, append a
`#L<start>` or `#L<start>-L<end>` line reference.

Parameters ~
{opts} `(optional)` `(YankFilePathOptions)` See |yank-file-path-api-options|.
Return ~
`(string|nil)` copied text, or nil when the line range is invalid.

------------------------------------------------------------------------------
                                  *yank-file-path-api-yank-file-path-code-block*
                     `M.yank_file_path_code_block`({opts})
Copy the current file path and buffer contents in a code block to the
clipboard.

With no range, include the entire buffer. With a valid range, include only
the specified lines.

Parameters ~
{opts} `(optional)` `(YankFilePathOptions)` See |yank-file-path-api-options|.
Return ~
`(string|nil)` copied text, or nil when the line range is invalid.


 vim:tw=78:ts=8:noet:ft=help:norl: